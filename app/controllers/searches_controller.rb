class SearchesController < ApplicationController
  def index
    #FIXME: We shall see if this is actually needed to load the json
    # @queries = User.
    # @queries.map{|q| "#{u.first_name} #{u}"}
  end
end
